#!/usr/bin/env bash

source "$HOME/.bash_profile"

tap=$(brew tap | gum filter --placeholder "Untap out of...")

if [[ -n "$tap" ]]; then
    brew untap "$tap"
    homebrew-update-brewfile
    gum-done
fi
