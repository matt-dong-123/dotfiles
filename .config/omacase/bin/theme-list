#!/usr/bin/env bash

fd --type d --type l --max-depth 1 . ~/.config/omacase/themes/ | sort | while read -r path; do
    theme=$(basename "$path")
    IFS='-'
    read -ra parts <<<"$theme"
    IFS=' '
    title="${parts[*]^}"
    echo "$title"
done
