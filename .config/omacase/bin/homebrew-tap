#!/usr/bin/env bash

if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

gum input --placeholder "Tap into..." | xargs brew tap
~/.config/omacase/bin/homebrew-update-brewfile
~/.config/omacase/bin/gum-done
