#!/usr/bin/env bash

if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

gum style --foreground 04 --border double --border-foreground 01 "Upgrading..."

brew upgrade --greedy
~/.config/omacase/bin/gum-done
