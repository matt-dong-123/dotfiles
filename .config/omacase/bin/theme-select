#!/usr/bin/env -S bash -l

fzf_args=(
    --preview 'theme-preview {}'
    --border-label '  Themes '
    --no-multi
)

theme=$(theme-list | fzf "${fzf_args[@]}")

if [[ -n "$theme" ]]; then
    theme-set "$theme"
fi
