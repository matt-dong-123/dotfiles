local colors = require("colors")

sbar.bar({
	topmost = "window",
	height = 40,
	color = colors.bar.bg,
	corner_radius = 15,
	padding_right = 10,
	padding_left = 10,
	position = "top",
	margin = 5,
})
